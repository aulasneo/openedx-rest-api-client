[tox]
envlist = py38, quality

[pytest]
addopts = --cov openedx_rest_api_client --cov-report term-missing --cov-report xml
norecursedirs = .* docs requirements

[testenv]
deps = 
	-r{toxinidir}/requirements/test.txt
commands = 
	pytest {posargs}

[testenv:quality]
commands = 
	pycodestyle --config=.pep8 openedx_rest_api_client
	pylint --rcfile=pylintrc openedx_rest_api_client
	twine check {distdir}/*

