# Requirements for test runs
-c constraints.txt

-r base.txt                # Core dependencies

ddt
edx-lint
freezegun
pycodestyle
pytest-cov                # pytest extension for code coverage statistics
responses
twine			  # to be able to run `twine check` as a part of
			  # quality tests to detect packaging related issues
